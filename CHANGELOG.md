# 变更日志

## 0.2.0

- 重构为提交信息、当前变更和提交历史三个 VS Code 原生分区，支持独立折叠和拖动调整高度。
- 当前变更和提交历史改用原生 TreeView，支持状态与目录分组、当前文件图标主题和仓库变更数徽标。
- 舍弃、编辑远程、拉取、推送和刷新操作迁移至原生标题栏，使用 VS Code 产品图标和中文提示。
- 新增单行提交元数据、本地 HEAD/远程引用、领先落后数量和原生层级的历史文件差异。
- 新增远程刷新、遵循仓库配置的拉取，以及非强制推送全部本地领先提交。
- 新增基于 VS Code Language Model API 的提交信息生成，支持精简、标准和详细三档密度，无需 Gitool API Key。
- 保留按文件精确提交、未选择暂存内容、废纸篓、远程 URL 修改和推送失败重试行为。

## 0.1.0

首个可安装版本：

- 新增独立的 Gitool 活动栏和“文件提交”侧边栏。
- 新增多仓库发现与单仓库选择。
- 新增已跟踪文件默认选中、未跟踪文件默认不选中的安全选择规则。
- 新增按完整文件提交，并保留未选文件原有暂存内容。
- 新增提交消息、文件差异查看、刷新、提交和提交后推送。
- 新增未跟踪普通文件二次确认后移入系统废纸篓。
- 新增无上游分支选择已有远程并建立同名上游。
- 新增已有远程 URL 修改、结果核对和敏感凭据遮蔽。
- 新增推送失败后的提交哈希展示和精确提交重试推送。
- 新增仓库状态版本校验、工作区信任校验和仓库级写操作锁。
- 新增提交失败恢复、提交路径核对以及中文错误状态。

当前限制：

- 仅支持桌面版 VS Code，不支持浏览器版 VS Code。
- 仅支持按完整文件选择，不支持文件内代码块提交。
- 一次只操作一个仓库。
- 只修改已有远程，不创建或删除远程。
- 游离 HEAD 状态不能执行“提交并推送”。
