# 变更日志

## 0.2.4

- 将精简、标准和详细三档选择迁移到 VS Code 原生 Quick Pick，不再受提交区域拖动高度限制。
- 保留生成按钮的当前档位星群，并在宿主选择器中展示档位名称、内容说明和当前选中状态。

## 0.2.3

- 修复生成内容菜单在提交区域高度不足时越过边界并被裁切的问题。
- 菜单现在按 Webview 可用空间自动选择向上或向下展开，空间不足时在边界内滚动。

## 0.2.2

- 模型选择按钮改为直接显示当前模型名称，窄侧边栏中自动截断并保留完整悬停说明。
- 优化精简、标准和详细三档星群，生成内容菜单新增名称、说明和选中状态。

## 0.2.1

- 新增按仓库记忆的 AI 模型选择，可在自动模式和 VS Code 当前可用模型之间切换。
- 自动模式优先使用 Copilot；显式选择失效时明确提示，不静默切换模型。

## 0.2.0

- 重构为提交信息、当前变更和提交历史三个独立区域，支持折叠、拖动、键盘调整、双击复位和按仓库持久化。
- 新增状态与目录分组、文件类型图标和当前仓库变更数徽标。
- 新增提交图、单行提交元数据、本地 HEAD/远程引用、领先落后数量和历史文件差异。
- 新增远程刷新、遵循仓库配置的拉取，以及非强制推送全部本地领先提交。
- 新增基于 VS Code Language Model API 的提交信息生成，支持精简、标准和详细三档密度，无需 Gitool API Key。
- 将提交区操作改为紧凑的 VS Code 原生图标，并新增 Gitool Marketplace Logo。
- 保留按文件精确提交、未选择暂存内容、废纸篓、远程 URL 修改和推送失败重试行为。

## 0.1.0

首个可安装版本：

- 新增独立的 Gitool 活动栏和“文件提交”侧边栏。
- 新增多仓库发现与单仓库选择。
- 新增已跟踪文件默认选中、未跟踪文件默认不选中的安全选择规则。
- 新增按完整文件提交，并保留未选文件原有暂存内容。
- 新增提交消息、文件差异查看、刷新、提交和提交后推送。
- 新增未跟踪普通文件二次确认后移入系统废纸篓。
- 新增无上游分支选择已有远程并建立同名上游。
- 新增已有远程 URL 修改、结果核对和敏感凭据遮蔽。
- 新增推送失败后的提交哈希展示和精确提交重试推送。
- 新增仓库状态版本校验、工作区信任校验和仓库级写操作锁。
- 新增提交失败恢复、提交路径核对以及中文错误状态。

当前限制：

- 仅支持桌面版 VS Code，不支持浏览器版 VS Code。
- 仅支持按完整文件选择，不支持文件内代码块提交。
- 一次只操作一个仓库。
- 只修改已有远程，不创建或删除远程。
- 游离 HEAD 状态不能执行“提交并推送”。
